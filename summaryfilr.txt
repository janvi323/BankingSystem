🔸 psql -U postgres -d loan_db      -- Connect to DB
🔸 \c loan_db                       -- Switch to DB
🔸 \l                              -- List DBs
🔸 \dt                             -- List Tables
🔸 \d loan                        -- Table structure
🔸 SELECT * FROM loan;            -- Show all rows
🔸 CREATE TABLE customer (...);   -- Create table
🔸 INSERT INTO customer (...)     -- Insert data
🔸 UPDATE customer SET ...        -- Update data
🔸 DELETE FROM customer WHERE ... -- Delete data
🔸 SELECT current_database();      -- Current DB
🔸 \du                            -- List users
🔸 \q                             -- Exit CLI
👉 1. API way (for Postman or frontend):
//ENDPOINTSSSSS
Register → POST /api/auth/register

Login → POST /api/auth/login

Apply Loan → POST /api/loans/apply


//register
{
  "fullName": "Samrth Kumar",
  "email": "samrth.umar@example.com",
  "phoneNumber": "9876543210",
  "address": "123, Green Street, Chandigarh, India",
  "role": "customer",
  "password": "Sam3",
  "confirmPassword": "Sam3"
}

//login

{
  "email": "samrth.umar@example.com",
  "password": "Sam3"
}
